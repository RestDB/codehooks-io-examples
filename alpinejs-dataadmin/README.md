# Alpine.js Data admin app

Example app with a SPA Alpine.js app to administer data in a Codehooks.io database.

# Add to an existing project

`coho init --empty`

# Install

`npm install`

# Deploy

`npm run deploy`